httpx>=0.27
pydantic>=2.6
# Optional, only needed by backends/llama_python.py:
# llama-cpp-python>=0.2.90
# Optional, only needed by backends/mlx_engine.py (Apple Silicon):
# mlx-lm>=0.18
# Optional, only needed by backends/vllm_engine.py (CUDA hosts):
# vllm>=0.6
# Optional, only needed by backends/exllamav2_engine.py (CUDA/ROCm hosts):
# exllamav2>=0.2
# Optional, only needed by backends/transformers_engine.py:
# transformers>=4.44
# torch>=2.2
