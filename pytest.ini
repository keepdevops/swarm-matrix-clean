[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -ra -q -m "not integration"
markers =
    integration: real-model tests; opt-in via `pytest -m integration`.
filterwarnings =
    ignore::DeprecationWarning
